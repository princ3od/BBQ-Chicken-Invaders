using System.Drawing;

namespace BattleShip
{
    class Chicken : GameObject
    {
        public Chicken(Image _texture, Point _location, Size _size, int _speed) : base(_texture, _location, _size, _speed)
        {
        }
    }
}
